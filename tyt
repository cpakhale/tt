parameters:
  - name: isProd
    type: boolean
    default: false

variables:
  myVar: ${{ if eq(parameters.isProd, true) }}: ProductionValue
  myVar: ${{ if eq(parameters.isProd, false) }}: DevValue

steps:
  - script: echo "myVar is $(myVar)"