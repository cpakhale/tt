We have noticed that one or more applications are still using the AKS proxy for the following endpoint. Please ensure that this endpoint is accessed through the appropriate route, as we plan to disable the AKS proxy next week. This change may impact your application if it currently relies on the AKS proxy for any endpoint.